const events = {
  createConnection: '@DATA_CHANNEL/CREATE_CONNECTION',
  setCurrentConnectionStatus: '@DATA_CHANNEL/CURRENT_CONNECTION_STATUS',
};

const stateKeys = {
  status: 'status',
};

export { events, stateKeys };
